apiVersion: apps/v1
kind: Deployment
metadata:
  name: skyways-saga-orchestrator
  namespace: skyways-core
  labels:
    app: skyways-saga-orchestrator
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: skyways-saga-orchestrator
  template:
    metadata:
      labels:
        app: skyways-saga-orchestrator
    spec:
      containers:
        - name: skyways-saga-orchestrator
          image: skyways/saga-orchestrator:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8086
            - containerPort: 9090
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "production"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: SAGA_DB_HOST
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: SAGA_DB_USER
            - name: DB_PASS
              valueFrom:
                secretKeyRef:
                  name: skyways-app-secrets
                  key: SAGA_DB_PASS
            - name: KAFKA_BOOTSTRAP
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: EUREKA_URL
            - name: SPRING_CONFIG_IMPORT
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: CONFIG_SERVER_URL
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: GCP_PROJECT_ID
            - name: JAVA_TOOL_OPTIONS
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: JVM_JMX_OPTS
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 90
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 45
            periodSeconds: 10
          resources:
            requests:
              cpu: "300m"
              memory: "384Mi"
            limits:
              cpu: "1000m"
              memory: "768Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: skyways-saga-orchestrator
  namespace: skyways-core
spec:
  selector:
    app: skyways-saga-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: 8086
    - name: jmx
      port: 9090
      targetPort: 9090
  type: ClusterIP
