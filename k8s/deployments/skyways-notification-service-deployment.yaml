apiVersion: apps/v1
kind: Deployment
metadata:
  name: skyways-notification-service
  namespace: skyways-core
  labels:
    app: skyways-notification-service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: skyways-notification-service
  template:
    metadata:
      labels:
        app: skyways-notification-service
    spec:
      containers:
        - name: skyways-notification-service
          image: skyways/notification-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8085
            - containerPort: 9090
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "production"
            - name: KAFKA_BOOTSTRAP
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: EUREKA_URL
            - name: SPRING_CONFIG_IMPORT
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: CONFIG_SERVER_URL
            - name: GCP_PROJECT_ID
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: GCP_PROJECT_ID
            - name: SENDER_EMAIL
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: SENDER_EMAIL
            - name: JAVA_TOOL_OPTIONS
              valueFrom:
                configMapKeyRef:
                  name: skyways-app-config
                  key: JVM_JMX_OPTS
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 90
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 45
            periodSeconds: 10
          resources:
            requests:
              cpu: "150m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: skyways-notification-service
  namespace: skyways-core
spec:
  selector:
    app: skyways-notification-service
  ports:
    - name: http
      port: 80
      targetPort: 8085
    - name: jmx
      port: 9090
      targetPort: 9090
  type: ClusterIP
